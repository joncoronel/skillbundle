# Implementation Plans

Generated by the improve skill on 2026-07-18, against commit `a0cea73`
(master). Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

Selection: the operator chose the top-5-by-leverage findings plus the
quick-win hygiene batch from a standard-effort full audit (4 parallel
auditors: correctness, security, perf/architecture, tests/deps/DX; every
finding vetted against the code before planning). No direction/feature plans
were requested this round.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Share tokens & urlIds via CSPRNG | P1 | S | — | DONE (executed 2026-07-18, commit `0192ed4`, PR from `plan-001`) |
| 002 | Typecheck script + CI gate | P1 | S | — | DONE (executed 2026-07-18, commit `a266376` on `advisor/002-ci-typecheck-gate`, awaiting merge) |
| 003 | Record content-fetch failures on thrown fetch | P1 | S | — | DONE (executed 2026-07-18, commit `0381319`, PR from `plan-003`) |
| 004 | Validate skill ids in install commands | P1 | M | — | DONE (executed 2026-07-18 as rev 2 — scope widened to detail pages + OG after executor STOP; commit `dc184d4`, PR from `plan-004`) |
| 005 | Repo-match Pro-gate tests | P1 | M | — | TODO |
| 006 | Bound `recordCopy` to public bundles | P2 | S | — | TODO |
| 007 | Bound `/dev` dashboard reads + compound index | P2 | S | — | TODO |
| 008 | `.env.example` + README env + AGENTS.md tagging fix | P2 | S | — | TODO |
| 009 | Drop fuse.js, align eslint-config-next | P2 | S | 002 (soft) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **002 first is recommended** (not required): it adds `pnpm typecheck`,
  which later plans' done criteria can use; every plan also works with the
  equivalent `npx tsc --noEmit`.
- 009's dependency on 002 is soft (verification convenience only).
- All plans are otherwise independent and touch disjoint files, so they can
  run in parallel worktrees. The only shared file across plans is this
  index — if executors run concurrently, have one party own the status
  updates.

## Findings considered and rejected

Recorded so nobody re-audits them next run:

- **Next.js preview pin (`16.3.0-preview.5`)** — deliberate, documented
  choice for the Cache Components migration (docs/architecture.md history
  note). Only the eslint-config skew is actionable (plan 009). Track 16.3 GA.
- **`/explore` "most starred" paginating over live `starCount`**
  (`convex/bundles.ts:647-653`) — real skip/duplicate potential during
  concurrent starring, but low-stakes for a read feed; fix would change feed
  semantics. Verdict: not worth doing; acceptable drift.
- **mermaid/dompurify npm advisories (via streamdown)** — unreachable:
  `components/markdown-content.tsx` overrides both `code` and `pre`
  renderers with shiki-based components, so Streamdown's mermaid path never
  mounts. Re-evaluate only if the markdown renderer stops overriding
  fenced-block rendering.
- **`@clerk/clerk-react@5.60` high advisory** — transitive-only (unused peer
  of `convex`; the app uses `@clerk/nextjs` v7 headless hooks). Unreachable.
- **`@conform-to/dom` high advisory** — only imported by a vendored
  cubby-ui component (`marching-border`), not by app form handling.
- **`ws` advisories (via convex)** — server/client lib internals, not
  app-controlled input paths.
- **visx `4.0.1-alpha.0` exact pins + `convex-test@0.0.51`** — known,
  accepted single-point risks; watch for stable releases, don't migrate now.
- **Committed `tsconfig.tsbuildinfo`** — auditor error: the file is
  git-ignored and untracked. No action.
- **`optimizePackageImports` additions** (radix-ui, simple-icons, motion) —
  LOW-confidence smell; needs bundle-analyzer measurement before any change.
  Investigate-grade, not planned.
- **Polar `listAll*` public endpoints scoping** (`convex/polar.ts:46-54`) —
  standard `@convex-dev/polar` surface, likely user-scoped internally;
  LOW-confidence investigate item. Verify against the component source if
  billing surface ever changes.
- **Reconcile head-of-line starvation, `ownsInstalls:false` curated
  handling, Typesense mark-and-sweep boundary, day-pinning across LA
  midnight** — all confirmed intentional per docs/skill-lifecycle.md.

## Vetted but unselected findings (candidates for a future round)

- Transient GitHub tree failures counted as discovery failures →
  wrongful-delisting path (`convex/skills.ts:784-826`, `:960`) — M effort,
  MED risk; the companion to plan 003.
- Overlapping discovery chains double-incrementing `discoveryFailCount`
  (no run-lock on `backfillDiscoverUrls`) — investigate-grade.
- Bundle page reads fat `skills` rows instead of `skillSummaries`
  (`convex/bundles.ts:441-476`) — needs `contentUpdatedAt` mirrored onto
  summaries first.
- Webhook handler tests (`convex/http.ts`); pure-lib client tests
  (`use-user-plan`, etc.).
- `convex/skills.ts` god-module split (L effort); run-once backfill
  deletion; skill-detail body dedupe (page vs sheet).
